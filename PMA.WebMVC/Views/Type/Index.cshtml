@model IEnumerable<PMA.Models.TypeListItem>

@{
    ViewBag.Title = "Genres";
}

<h2>Genres</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { TypeId=item.TypeId }) |
            @Html.ActionLink("Details", "Details", new { TypeId=item.TypeId }) |
            @Html.ActionLink("Delete", "Delete", new { TypeId=item.TypeId })
        </td>
    </tr>
}

</table>
